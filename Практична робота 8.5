#include <iostream>
#include <stdio.h>
using namespace std;
void main()
{
    long int b, i, power;
    printf("\nEnter the Base : ");
    cin >> b;
    printf("\nEnter the Index : ");
    cin >> i;
    power = pow(b, i);
    printf("\nPower %d^%d = %d", b, i, power);
}
