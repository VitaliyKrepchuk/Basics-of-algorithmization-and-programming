#include <iostream>
#include <clocale>
using namespace std;

int main()
{
	setlocale(LC_ALL, "UKR");
	int a, b, z;
	do
	{
		do
		{
			cout << "Введiть години (вiд 00 до 23)\n";
			cin >> a;
		} while (a < 0 || a>23);
		do
		{
			cout << "Введiть хвилини (вiд 00 до 59)\n";
			cin >> b;
		} while (b < 0 || b>59);
		if (a >= 7 && a < 13 && b >= 0 && b <= 59 || a >= 15 && a < 19 ||a==14 && b>=50)
		{
			cout << " Можна вiдвiдати найближчу крамницю;\n";
		}
		if (a >= 13 && a < 14 && b < 30)
		{
			cout << " Зараз краще дiйти до гастронома\n";
		}
		if (a == 13 || a == 14 && b > 0 && b <= 45  || a >= 19 && a < 23)
		{
			if (a == 22 && b > 10)
			{
				cout << " Зараз краще залишитися вдома, оскiльки всi магазини зачинено.\n";
			}
			else
			{
				cout << " Зараз краще рушати в супермаркет\n";
			}
		}
		if (a == 23 || a >= 0 && a < 7 && b >= 0)
		{
			cout << " Зараз краще залишитися вдома, оскiльки всi магазини зачинено.\n";
		}
		cout << "\n";
		cout << "Виконати програму ще раз ?\n";
		cout << "1.Так;\n";
		cout << "2.Нi;\n";
		cin >> z;
		cout << "\n";
	} while (z == 1);
}
